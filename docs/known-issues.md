# 已知問題

本文檔記錄 govmo-news 的已知問題和限制。

## 🔴 嚴重問題

目前無嚴重問題。

## 🟡 已知限制

### 1. 去重邏輯

**現狀：** 使用 RSS feed 中的 `guid` 字段進行去重

**影響：**
- ✅ 可靠，基於新聞局提供的唯一標識
- ⚠️ 如 RSS guid 格式變更，可能影響去重

**追蹤 Issue：** 待建立

## 🟢 輕微問題

### 1. 日誌文件（已解決）

**現狀：** 日誌文件已加入 `.gitignore`，不提交到 Git

**影響：**
- ✅ 不影響 Git 倉庫大小
- ⚠️ 本地運行時仍會產生日誌

**緩解措施：**
- 手動清理：`rm data/*.log`

### 2. 無監控告警

**問題：** 抓取失敗時無自動通知

**影響：**
- 可能長時間未發現抓取失敗

**緩解措施：**
- 定期檢查日誌
- 檢查 `data/processed/` 是否有新數據

**計劃：** 添加監控和告警機制

**追蹤 Issue：** 待建立

## ✅ 已確認事項

以下事項已確認：

| 事項 | 決定 | 日期 |
|------|------|------|
| 去重標準 | 使用 RSS guid | 2026-04-14 |
| 數據保留 | 永久保存 | 2026-04-14 |
| 前端需求 | 不需要前端 | 2026-04-14 |
| JSON 格式 | 每天一個 JSON 檔（array） | 2026-04-14 |
| 原始 XML | 不保存 | 2026-04-14 |
| 日誌文件 | 加入 .gitignore | 2026-04-14 |

## 📝 報告新問題

如發現新問題，請：

1. **檢查是否為已知問題** - 查看本文檔
2. **收集信息**：
   - 錯誤訊息
   - 重現步驟
   - 環境信息（OS、Python 版本等）
   - 日誌片段
3. **提交 Issue**：https://github.com/john-fb-agent/govmo-news/issues

### Issue 模板

```markdown
## 問題描述

[簡短描述問題]

## 重現步驟

1. [步驟 1]
2. [步驟 2]
3. [步驟 3]

## 預期行為

[應該發生什麼]

## 實際行為

[實際發生什麼]

## 環境信息

- OS: [例如：Ubuntu 20.04]
- Python: [例如：3.8.10]
- 版本：[例如：1.0.0]

## 日誌

```
[相關日誌片段]
```

## 其他信息

[任何其他相關信息]
```

## 📊 問題統計

| 狀態 | 數量 |
|------|------|
| 嚴重問題 | 0 |
| 已知限制 | 1 |
| 輕微問題 | 1 |
| 已確認事項 | 6 |
| 已解決問題 | 2 |

## 🔄 本文件何時需要更新

以下情況需要更新本文件：

1. **新問題發現** - 添加新的已知問題
2. **問題解決** - 將已解決的問題移至「已解決」章節
3. **限制解除** - 當限制被解決時更新
4. **待確認事項確認** - 更新待確認事項的狀態

## ✅ 已解決問題

| 問題 | 解決日期 | 解決方式 |
|------|---------|----------|
| JSON 文件覆蓋 bug | 2026-04-15 | 改為合併模式（載入現有 → 合併 → 保存） |
| `openclaw agent --session-id` 旗標錯誤導致 8AM cron 跑失敗 | 2026-06-24 | 改用 `--session-key agent:main:main`（commit `da322ce`） |

| 問題 | 解決日期 | 解決方式 |
|------|---------|---------|
| 無 | - | - |

---

## 🔧 已解決問題詳情

### `openclaw agent` CLI 旗標陷阱（2026-06-24 fix，commit `da322ce`）

**症狀：** 2026-06-23 早上 8AM daily-summary cron 第一次跑失敗，三個 batch 全部報錯：

```
Invalid session ID
```

**根因：** `src/generate_summary.py` 的 `call_openclaw()` 用了 `--session-id` 旗標呼叫 `openclaw agent` CLI，但 Gateway 實際接受的旗標是 `--session-key`。

**修正：** `src/generate_summary.py` line 39

```diff
-        "--session-id", "agent:main:main",
+        "--session-key", "agent:main:main",
```

**驗證：** 修正後重跑，14 則新聞全部成功分類、AI 摘要、HTML 寫入、index 重建、commit + push 到 main 都正常完成。

**給未來 coder agent 的提醒：**
- `openclaw agent` 的 session 識別旗標是 `--session-key`，不是 `--session-id`。
- 修改後第一次跑失敗看到 "Invalid session ID" 時，先確認旗標名稱（`openclaw agent --help`）再排查。
- 詳見 `repo-agent.md` 的「🚧 執行注意事項」section。

---

**最後更新：2026-06-24  
**維護者：** john-fb-agent  
**版本：** 1.12.1
